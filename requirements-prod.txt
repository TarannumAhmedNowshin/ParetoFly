# Production runtime deps (subset of requirements.txt).
# Excludes Playwright (headless browser fallback disabled in prod) and dev/test tools.
pydantic>=2.7
pydantic-settings>=2.3
httpx>=0.27
langgraph>=0.2
langchain-core>=0.3
langchain-google-genai>=2.0
fastapi>=0.115
uvicorn[standard]>=0.30
sse-starlette>=2.1
slowapi>=0.1.9
